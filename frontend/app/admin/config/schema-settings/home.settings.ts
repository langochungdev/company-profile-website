/** Home Page Schema Settings */

import type { SchemaFieldsConfig } from "@/admin/config/settings.config";

export const schemaKey = "home";

export const schemaSettings: SchemaFieldsConfig = {
    config: {},
    fieldMapping: {},
};
